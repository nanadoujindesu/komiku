# Lightweight image WITHOUT Chromium.
# Use this when you don't need USE_BROWSER=true.
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --production --ignore-scripts --omit=optional

COPY . .

EXPOSE 3000
ENV PORT=3000

CMD ["node", "server.js"]
